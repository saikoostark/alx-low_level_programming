#include <stdio.h>
#include "main.h"

/**
 * _strcpy - function
 * @dest: array
 * @src: size
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */

char *_strcpy(char *dest, char *src)
{
	dest = src;
	return (dest);
}
